require 'rubygems'
require 'rspec'
require "bundler/gem_tasks"


namespace :test do

	NO_ANOREXIC_AUTO_START = true
	ENV['RACK_ENV'] = 'test'
	require './lib/anorexic.rb'

	# require 'lib/anorexic/feed_haml.rb' if require 'haml'
	# require 'rubygems'


	# desc "Test that default listen port increments"
	# task :anorexic_app do
	# 	RSpec.describe Anorexic do
	# 		describe "listen" do
	# 			it "increments the port to be used for every listen call"  do
	# 				a = []
	# 				10.times { a << listen}
	# 				expect(a[-1].port - a[0].port).to eq(9) # (had to increment 9 times - the first time increments nothing)
	# 			end
	# 		end
	# 	end
	# end

	
end